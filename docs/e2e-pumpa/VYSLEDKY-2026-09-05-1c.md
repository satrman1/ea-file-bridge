# E2E transportu PUMPA — dokončení, běh 2026-09-05 (vlákno Z260904-1c)

Navazuje na `VYSLEDKY-2026-09-05.md` (běh 1b, 20:46–21:19). Tento běh: **22:43–22:59**, souběžně Miloš u EA + Claude nad `responses\res-*.json`. Kód před během: `52d9b39` + D1 nasazena 22:33 (`res-D1.json`: `updated: ["EA_OnOutputItemDoubleClicked","FB_OpDelete"]`), EA plně restartována. Harness před během 221/221, po běhu **223/223**.
Prostředí: `EAExample.qea`, EA 17.1.5, pumpa v0.5. Zdroje pravdy: `responses/res-P4|P5c|P5d|P8c|P8d|D2.json` (mimo git), `requests/processed/req-*.<čas>.json`, řádky Output tabu vložené Milošem do chatu.

## Tabulka výsledků

| Krok | Co ověřuje | Výsledek | Doklad (soubor + klíč) |
|---|---|---|---|
| 1 | P4 znovu — vstupní balíček, UC, diagram | ✅ | `res-P4.json` (běh 22:43, přepsán během 22:55 — processed kopie `req-P4.20260905-224326.json`): `status: done`, 6/6 `ok`, `warnings` žádné; `results[0]` pkg `{1ED84D5D-4113-4bcc-9013-B996BB329473}` id 1077, `results[1]` UC `{C4A8EB9C-DF03-482b-A3C5-CAC19AC33C45}` id 11424, `results[4]` diagram `{43D41310-44AE-45f8-BC1A-1FF30EC50D9A}` id 1160; `risk.confirm.timestamp 22:43:24`. QC `nalez` = QC-DEMO-001 (lifeliny 11061/11062, známé z 1b). |
| 2 | Dvojklik na řádek package a diagram (nález P7 z 1b) | ✅ **rozhodnuto: funguje** | Output tab (vložil Miloš, viz citace níže): marker dorazí celý, `ShowInProjectView zavolano`, **balíček i diagram se v Project browseru označily**. Žádná změna kódu. |
| 3a | Chyba uprostřed dávky (P5 správně) | ✅ | `res-P5c.json`: `status: error`; `results[0]` `ok`, UC `{7F38D4EA-856D-41f9-B3D2-7884B0D423E4}` id 11426 `created: true`; `results[1]` `E_NOT_FOUND` „Element nenalezen: {0000…}"; `risk.riskReasons[1]` „Neresolvovatelny target … fail-closed"; confirm 22:47:05. Popup ukázal důvod, Ano dán záměrně. |
| 3b | Oprava z res (P5b správně) | ✅ | `res-P5d.json`: `status: done`, `results[0].count 1`, `items[0].guid {0B9D970A-1116-4958-A5E7-DB6AF8D7526D}`, `steps: 2`, `readback.api[0].steps` 2 kroky (system/actor). Miloš v EA: **1 scénář, 2 kroky** ✓. |
| 4a | Mazání s více cíli, jeden neexistuje — response | ✅ | `res-P8c.json`: `status: error`, `results[0].code E_NOT_FOUND`, `message` „targets[1]: package nenalezen \| smazano pred chybou: 1 z 2, neprovedeno: 0 (vycet v items)"; `items[0]` `{type Package, id 1077, name "P4 Mail (pumpa)", path "Example Model.Model Based Add-Ins.EA Addins.#FB-TEST.P4 Mail (pumpa)", deleted true}` (oprava z 5. 9. ✓); `items[1]` `deleted false, code E_NOT_FOUND, index 1`. Popup: řádek `Kde (plna cesta mazaneho): Example Model.Model Based Add-Ins.EA Addins.#FB-TEST.P4 Mail (pumpa)` (screenshot Miloše), `risk.summary.paths[0]` shodné. Balíček z browseru zmizel ✓. |
| 4b | Mazání s více cíli — Output tab | ❌ → **opraveno + ✅ živě (P8d)** | P8c: v Output **žádný** řádek `[smazano]` (jen `FB P8c -> error: 1 ops (0 ok, 1 chyb)`). Příčina v kódu (nález N7 níže). Po nasazení D2 (`res-D2.json`: `updated: ["FB_LogChanges","FB_Main"]`, 22:55:00) + P4 znovu (pkg 1078 `{324B620B-7F67-47ee-80ED-E8BB26005AE0}`) + P8d: `res-P8d.json` tvarově shodné s P8c (`items[0].id 1078`, `deleted true`; `items[1]` `E_NOT_FOUND index 1`), **Output**: `[smazano] Package "P4 Mail (pumpa)" @ Example Model.Model Based Add-Ins.EA Addins.#FB-TEST.P4 Mail (pumpa)` + `[nesmazano] Package "{0000…}" (E_NOT_FOUND)`; navíc `qc.status: ciste` (u P8c `qc` chybělo — QC se v této větvi dřív vůbec nespouštělo). Balíček zmizel ✓. |

**Bilance:** všechny tři opravy z iterace 7 (1a proklik per typ, 1b cesta v dialogu, 1c chybová větev delete s více cíli) jsou **živě doloženy pumpou**; P5 řetěz „ok → chyba → oprava z res" doložen pumpou (v 1b padl na obsluze). Jeden nový nález (N7) opraven a doložen v témž běhu. Stav `#FB-TEST`: zpět na 9 podpackages (P4 Mail 1077 i 1078 smazány).

## Nález P7 — rozhodnutí (krok 2)

Citace Output tabu (Miloš, 22:44, `FB_Config.navProbe: true`):

```
dblclick debug v6: tab='AI Bridge' id=0 how=[val,val,val] keys1=[val] keys3=[val] len=124 head='  [vytvoreno]  package "P4 Mai' tail='ins.#FB-TEST.P4 Mail (pumpa)  (pkg:1077)'
dblclick nav: marker=(pkg:1077) kind=pkg target=1077
dblclick nav: pkg 1077 -> ShowInProjectView zavolano
dblclick debug v6: tab='AI Bridge' id=0 how=[val,val,val] keys1=[val] keys3=[val] len=54 head='  [diagram vytvoren]  "P4 Mail' tail='voren]  "P4 Mail UC (pumpa)"  (dgm:1160)'
dblclick nav: marker=(dgm:1160) kind=dgm target=1160
dblclick nav: dgm 1160 -> ShowInProjectView zavolano
```

Miloš: „prokliky fungují" — balíček i diagram označeny.

Vyhodnocení proti variantám zadání: **žádná z a)/b)/c) nenastala.** `LineText` dorazí celý (len 124 / 54, marker v `tail`), `GetPackageByID`/`GetDiagramByID` objekt našly, `ShowInProjectView` je z add-in kontextu **označí** (vyvrací hypotézu 1 z N1 v 1b — pro Package i Diagram). Kód navigace se mezi v5 (běh 1b, ❌) a v6 (tento běh, ✅) **nezměnil** (v6 přidal jen debug řádky). Příčina selhání v 1b tedy není v kódu; nejpravděpodobnější je stav EA runtime v 1b (dvojklik jde přes EA runtime, §1a — P1 nasadila handler pumpou a EA byla restartována, ale debug v5 tehdy tiskl `id=0` a řádek uříznutý na 30 znaků, takže o markeru nic neříkal; dnes běžela EA po plném restartu za D1). **Neopakovalo se → uzavřeno jako ověřené; bez opravy.** Kdyby se to vrátilo: první krok = tytéž tři debug řádky (v6 zůstává v kódu, zapíná `navProbe`).

## Nález N7 — částečné mazání není v Output tabu (chyba kódu, opraveno, ✅ živě P8d)

Co se stalo: P8c smazala balíček 1077 (res `items[0].deleted: true`), ale Output tab ukázal jen souhrn `FB P8c -> error: 1 ops (0 ok, 1 chyb)` — žádné `[smazano]`. Uživatel v EA neviděl, že v modelu reálně ubyl balíček.

Příčina (dvě místa): `FB_Main` volal `FB_LogChanges` (a QC + `lastwrite` pro Add-in Search `FB_Changes`) jen při `okc > 0` — jednooperační delete dávka s chybou má `okc = 0`; a `FB_LogChanges` výsledky se `status != ok` přeskakoval, i když nesly `items` s `deleted: true`.

Oprava (`52d9b39` → tento commit): `FB_Main` — `partialDel` = některý `delete_from_model` má v `items` `deleted: true` → blok výpisu/QC/lastwrite běží i při `okc = 0` (+11 řádků). `FB_LogChanges` — u `delete_from_model` bere i `status: error` s `items`; `deleted: true` → `[smazano] Typ "jméno" @ cesta`, `deleted: false` → nový řádek `[nesmazano] Typ "cíl" (kód)`; error bez `items` = beze změny (nic) (+9 řádků). Harness +2 testy (`FB_LogChanges: delete error s deleted:true …`, `FB_Main: delete error po castecnem smazani …`) → **223/223**. Nasazení D2 (`deploy_src` `FB_Main` + `FB_LogChanges`, pumpa se sama přenačetla, bez restartu EA) a ověření P8d v témž běhu — viz tabulka 4b.

## Co se změnilo v repu (tento commit, push NE)

- `src/AICodeBridge.FB_Main.js` — `partialDel` (N7).
- `src/AICodeBridge.FB_LogChanges.js` — delete error s items → `[smazano]`/`[nesmazano]` (N7).
- `test/harness.js` — +2 testy → 223/223.
- `docs/PROTOKOL-EAFB.md` — §9c tabulka: package/diagram ✅, nový řádek `[nesmazano]`, 1c ✅ živě + N7; §11 harness 223; §12 odstavec „Stav 5. 9. 2026" (105 operací, 9 podpackages).
- `docs/e2e-pumpa/ready/req-P5c|P5d|P8c|P8d|D2.json` — dávky běhu s dosazenými GUIDy (záznam); `req-D1.json` (z 1b, dosud netrackovaný).
- tento soubor.

## Zbývá (Miloš)

- Pá 11. 9.: sanitizace + tag + push (beze změny plánu; commit tohoto vlákna je lokálně).
- `#FB-TEST`: žádný úklid navíc (P4 Mail balíčky smazány testem; `FBT *` a dva UC balíčky rozhoduje Miloš — stav z 1b).
- Kandidát do backlogu (ne zápřah): `FB_Main` odmítat request s literálem `GUID-` (N3 z 1b) — dnes to nebylo potřeba, placeholdery dosazoval Claude.
