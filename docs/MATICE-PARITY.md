# Matice parity MCP × ea-file-bridge (stav 2026-08-20, po iteraci 5; dávky 20260818-* + 20260819-* + 20260820-*)

Legenda: ✅ E2E ověřeno (v závorce důkazní dávka) · 🔵 nad rámec MCP · ⛔ vědomě vynecháno

| MCP tool | Operace bridge | Stav |
|---|---|---|
| — | `ping` | ✅ |
| (chybí v MCP) | `query` (SQL read-only, vrací GUIDy) | ✅ 🔵 |
| `find_elements_by_name` | `find_elements_by_name` | ✅ |
| `find_packages_by_name` | `find_packages_by_name` | ✅ |
| `get_elements_information` | `get_elements_information` | ✅ |
| `get_packages_information` | `get_packages_information` | ✅ |
| `get_connectors_information` | `get_connectors_information` | ✅ |
| `get_diagrams_information` | `get_diagrams_information` (vč. `messages`) | ✅ |
| `create_or_update_elements` | `create_or_update_elements` | ✅ |
| `create_or_update_package` | `create_or_update_package` | ✅ |
| `create_or_update_connectors` | `create_or_update_connectors` | ✅ |
| `create_or_update_attributes` (K2) | `create_or_update_attributes` | ✅ |
| `create_or_update_operations` (K2) | `create_or_update_operations` | ✅ |
| `create_or_update_messages` (K1) | `create_or_update_messages` | ✅ |
| `delete_from_model` (K4) | `delete_from_model` | ✅ všech 7 typů: Connector (20260816-12/14), Parameter/Attribute/Operation/Element/Package (20260817-04), Diagram (20260817-07) |
| `delete_taggedvalue_from_model` (K4) | `delete_taggedvalue_from_model` | ✅ ElementTV + ConnectorTV vč. `operation_guid` UNDO drillu T1 (20260817-03) |
| `remove_elements_from_diagram` (K4) | `remove_elements_from_diagram` | ✅ element zůstal v modelu, §11 (20260817-03) |
| (MCP: `-enableDelete`/`-enableEdit`) | **whitelist operací `FB_OpsAllowed`** | ✅ 🔵 (E_OP_FORBIDDEN, 20260816-16) |
| `create_baseline` (K5) | `create_baseline` (pojmenovaná `AI-pre-<session>-<batch>`) | ✅ 🔵 jméno dohledatelné v `get_baselines` — MCP jméno neuměl (20260817-02) |
| (chybí v MCP) | `get_baselines` | ✅ 🔵 |
| (chybí v MCP) | `baseline_diff` | ✅ 🔵 summary + raw comparelog; diff vykázal cílenou změnu (20260817-02) |
| `clone_package` (K3) | `clone_package` (kvóta V3, `volume`) | ✅ vč. diagramů podstromu (20260817-05) |
| `clone_elements` (K3) | `clone_elements` (kvóta V3) | ✅ (20260817-05/-07); `E_QUOTA` na 101 el. bez confirm (20260817-14). ⚠ `Element.Clone()` NEPŘENÁŠÍ owned diagramy (ověřeno -07: zdroj 1 → klon 0) — limit MCP éry platí i pro Automation; bridge počet aspoň vykazuje (`ownedDiagrams`). Náhradní postup: diagramy klonu postavit Diagram Builderem (`create_or_update_diagram` + `place_elements_on_diagram` — od iterace 2 ✅ k dispozici). |
| `import_element_linked_documents` (K10) | `import_element_linked_documents` | ✅ round-trip s exportem, marker zachován; EA RTF normalizuje (20260817-08) |
| `export_element_linked_documents` (K10) | `export_element_linked_documents` | ✅ (20260817-08; přes FB_ComObj re-test 20260817-22) |
| `layout_connectors` (K11) | `layout_connectors` | ✅ (20260817-12) — po opravě mapy stylů: EA LinkLineStyle jen 1–9, orthS=8/orthR=9 (bug 10/11 tiše degradoval na custom; fix nasazen deploy_src 20260817-11) |
| `change_connector_visibility` (K11) | `change_connector_visibility` | ✅ Hidden 0→1→0 v `t_diagramlinks` (20260817-09) |
| `open_diagrams` (K11) | `open_diagrams` | ✅ (20260817-09) |
| `reload_diagrams` (K11) | `reload_diagrams` | ✅ (20260816-14) |
| `create_or_update_diagram` | `create_or_update_diagram` | ✅ create v package i pod elementem (owningElement), MDG typ přes `StyleEx MDGDgm=` (parita s MCP referencí 20260818-02/-03), update jména/vlastností, §7e Author+Version na create (20260818-05) |
| `place_elements_on_diagram` | `place_elements_on_diagram` | ✅ x/y/width/height/style + auto-mřížka bez souřadnic (20260818-05); konektor mezi umístěnými konci EA vykreslí sám — response `connectorsOnDiagram` z `t_connector` (Dependency 4802, 20260818-07; ⚠ `t_diagramlinks` u čerstvého diagramu prázdná) |
| `get_diagram_image` | `get_diagram_image` | ✅ 🔵 **PNG do souboru** `<baseDir>\responses\images\` (MCP uměl jen inline — bolest EDU pipeline); `inline:true` → `png_b64` 1:1 (20260818-08); po zápisu zpráv nutný `reload_diagrams` před exportem (20260818-10) |
| `find_element_in_diagrams` | — | ⛔ kryje `query` nad `t_diagramobjects` (rozhodnutí zadání) |
| `apply_baseline` | — | ⛔ **trvale** (§12a emr-zapis-pravidla, kap. 6 zadání) |
| `get_current_elements` / `get_current_package` / `get_current_diagram` | `get_selected_context` (**v0.10**, revize verdiktu „bezpředmětné" — interaktivní režimy výběr zhodnotí) | 🔨 🔵 nad rámec MCP navíc: tečková `path`, `branchGuid` pro scope, `inWhitelist`, multi-výběr; harness 5 testů; živě 20260821-* |
| `select_element_in_browser` / `_in_diagram` | — (COM navigace z add-inu = past §1a/4); náhrada = NATIVNÍ dvojklik v Output tabu + výsledky `FB_Changes` (iterace 5 B) | ⛔ / 🔵 viz sekce Iterace 5 |
| (nad rámec MCP) | `update_diagram_properties` (K6 Author/Version/ShowDetails/StyleEx) | ✅ 🔵 (20260817-13) |
| (nad rámec MCP) | `set_diagram_object_style` (K9 barvy objektu + reset) | ✅ 🔵 (20260817-13) |
| (nad rámec MCP) | změna `Element.Type` (K7) — součást `create_or_update_elements` | ✅ 🔵 Object→Component→Object (20260817-13) |
| (nad rámec MCP) | `isComposite` + `SetCompositeDiagram` (K8) — součást `create_or_update_elements` | ✅ 🔵 NType=8 (20260817-13) |
| (nad rámec MCP) | `create_or_update_scenarios` (strukturované UC scénáře do Scenarios tab, deterministický rebuild V2d) | ✅ 🔵 pořadí+typy kroků (actor/system) + větve Alternate/Exception s napojením na kroky (20260818-28; UI potvrzeno). ⚠ Step API reorder lekce — mechanika výhradně přes `XMLContent` (spike -18…-26, §6b protokolu) |
| (nad rámec MCP — **MCP protějšek neexistuje, bridge-only**) | `create_or_update_constraints` (internal constraints do záložky Constraints / `t_objectconstraint` — PRE/PST/ASU na UC dle U2 rev., deterministický rebuild V2d) | ✅ 🔵 create 3× + rebuild `removed: 3` s propsanou úpravou, readback `t_objectconstraint` 3/3 (20260819-02…04, §6c protokolu) |
| (nad rámec MCP) | `apply_classifier_stereotypes` (port ITAN-Apply Classifier Stereotypes on SD.vbs) | ✅ 🔵 Object→Component + stereotyp classifiera; idempotence 2. běh = 0 změn (20260818-31) |
| (nad rámec MCP) | `find_or_create_referencing_sr` (port ITAN scaffoldu, SR větev; šablony per-repo `FB_ScaffoldConfig`) | ✅ 🔵 1. běh založil 1 pkg + 4 diagramy + 4 elementy + 3 vazby + TV 505-1 (20260818-35), 2. běh našel a nic nezaložil (20260818-36) |
| (nad rámec MCP) | `deploy_src` (dev nasazení kódu executoru) | ✅ 🔵 vč. založení NOVÉ operace ze signatury (FB_ComObj, 20260817-22); v bance deny |
| (nad rámec MCP) | **GUI fallback** `FB_ProcessFolder` + menu „Process requests (File Bridge)" | ✅ 🔵 response bez běžící pumpy, archiv processed\ (20260817-23). ⚠ EA runtime: nutný `FB_ComObj` (žádný ActiveXObject/Enumerator) a aktivace kódu = **plný restart EA** (Reload Current Project nestačí) |

**K9 legenda diagramu**: neimplementována — kandidát, odhad 0,5 dne (vlastní struktura legend elementu v EA, netriviální mapování na protokol). Fallback: ITAN skript.

**Dostavba idempotence dle auditu B2 (2026-08-18, dávky 20260818-40…55; spec = `AUDIT-B2-IDEMPOTENCE-2026-08.md` §5, docs v0.5):** sonda názvů sloupců přes `sqlite_master` (-40), deploy 5 souborů vč. nového helperu `FB_DedupFind` (-41), setup K2A 11173 / K2B 11174 (-42). Důkazy:

- **(a) K2 `match: "composite"`**: táž konektorová dávka 2× — 1. běh `created: true` (Dependency «use» 4819, -43), 2. běh `created: false` + `matchedBy: "composite"` + týž GUID, SQL readback `COUNT(*)` v `t_connector` = 1 beze změny (-44).
- **(b) K1 `matchByName`**: táž dávka 2× — `FBT B2 MBN` → 1 element (11178; 2. běh `matchedBy: "name"`), `FBT B2 NOMATCH` **bez** matchByName → 2 elementy (11179 + 11182 = důkaz nezměněného defaultu), package `FBT B2 PKG` → 1 (1062, `matchedBy: "name"`); COUNT readbacky 1/2/1 (-45/-46).
- **(c) K4 `dedupKey`**: create s `FBT-B2-DK1` → el. 11184 + TV `ai.dedup` (-47) → přejmenování GUIDem na „FBT B2 DK prejmenovano" (-48) → **táž dávka znovu** → UPDATE původního GUID (`created: false`, `matchedBy: "dedupKey"`), žádný nový element (COUNT dle ai.dedup = 1, dle jmen = 1) (-49) — klíč přežil přejmenování.
- **(d) K3 `rebuild: true`**: diagram `FBT B2 Seq` 1155 + lifeliny 11188/11189 (-50); rebuild 3 zpráv s explicitními seqNo: 1. běh `removed: 0` + 3 created (-51), 2. běh `removed: 3` + 3 recreated, COUNT Sequence konektorů diagramu = 3 v obou bězích (-53); PNG diagramu po obou bězích **byte-identické** (SHA-256 `e15bfcbb…` shodný, -52/-54).
- **(e) regresní dávka vzoru 20260818-11**: shodné chování — ping + query (model 70 ops) + create (el. 11195) + `E_WHITELIST` na pkg 1052 + skipped (-55).
- **(f)** grep `ActiveXObject` na nových/změněných souborech (FB_DedupFind, FB_OpConnectors, FB_OpElements, FB_OpPackage, FB_OpMessages) = 0.

Testovací artefakty FBT-B2 k úklidu viz PROTOKOL §12.

**Regresní běh 2026-08-17** (bod 4 zadání): pozitivní ping+query+create_or_update_elements + `E_WHITELIST`+skipped (20260817-15), `E_REPO` na dávce pro EMR_PROD — nic neprovedeno (20260817-16), `E_SQL_READONLY` na DELETE (20260817-17). Shodné chování jako 2026-08-16. **Regrese po iteraci 2**: ping + query + create (el. 11123) + `E_WHITELIST` na pkg 1052 + skipped — shodné chování (20260818-11). **Regrese po iteraci 2b**: ping + query + create (el. 11163) + `E_WHITELIST` na pkg 1052 + skipped — shodné chování (20260818-37).

**Sekvenční řetěz Diagram Builder + K1** (dosud šly messages jen na předpřipravený diagram 1131): nový MDG Sequence diagram + place lifelin 11061/11062 + `create_or_update_messages` v jedné dávce přes `$N`, PNG důkaz po reloadu (20260818-09/-10, diagram 1140).

**Iterace 4b — Risk Gate, výpočetní část (2026-08-19, dávky 20260819-05…19; spec = `IT-ANALYSIS/Zadani-EA-File-Bridge-Iterace-4b-Risk-Gate.md` v1.1, PROTOKOL §6d):** deterministické metriky + klasifikace zápisových dávek (`FB_RiskGate`), politika `FB_RiskPolicy` (vzor FB_OpsAllowed, hodnoty limitů = výchozí konfigurace schválená Milošem 2026-08-19), čistá JS `FB_Sha256` (FIPS vektory ověřeny lokálně, E2E shoda s `sha256sum` req souboru). Vynucen jen BLOCKED; ELEVATED shadow. MCP protějšek neexistuje — bridge-only 🔵. Důkazy:

- **(a) T5-1 LOW**: create 2 elementů → `riskLevel: LOW`, writeOps 2, affectedPackages 1, exekuce beze změny; audit tagy `ai.risk.*` na Artifactu (-07, čtení -19).
- **(b) T5-6 vč. negativu B3**: Č dávka bez `risk` (-08); `$N` na create větev → `updatedExisting: 0` (-09); **`$N` na výsledek `find_elements_by_name` → `updatedExisting: 1`** (-10 — důkaz opravy B3).
- **(c) T5-4 fail-closed**: neresolvovatelný guid → ELEVATED „Neresolvovatelny target“ + exekuce E_NOT_FOUND (-11).
- **(d) T5-5 hard limit**: 501 syntetických writeOps → `E_RISK_BLOCKED`, results `skipped`, soubor v `rejected\`, audit zapsán, `COUNT(*) FBT RG BULK% = 0` (-12, kontrola -19). Klasifikace 501 ops = 24 ms.
- **(e) T5-12 B4**: `create_or_update_elements` se jménem `FB_RiskPolicy` → `E_RISK_BLOCKED` (-13) — dvoudávkový útok na politiku nemožný.
- **(f) T5-11 W9**: politika bez položky pro repo → vše ELEVATED, `policyValid: false` (-14/-15); obnova + regrese LOW (-16/-17).
- **(g) T5-9 část (runtime pumpy)**: `FB_Sha256` 62 kB = 143 ms, 500 kB = 1300 ms; gate běžné dávky 4–11 ms (-07…-18). EA runtime měření zbývá (restart EA).

**Iterace 4b V2 — Risk Gate confirm okruh (2026-08-19, dávky 20260819-22…41; spec = zadání 4b v1.1 §3/§6, PROTOKOL §6e):** ELEVATED přestává být shadow — vynucené lidské potvrzení nad skutečným payloadem. Kontrakt `FB_Main` = cesta k souboru (I5, hash surových bajtů + parse z jednoho čtení), `requests\pending\`, jednorázový nonce jen v `res-*.json` (B2), potvrzení výhradně `FB_ConfirmPending` (mimo registr operací, B1), bezstavová re-klasifikace (W1), migrace E_QUOTA (W6). MCP protějšek neexistuje — bridge-only 🔵. Před nasazením ověřeno offline harness (51/51 PASS, Node mock COM/EA — zachytil mutaci vstupního pole ve `FB_Sha256`). Důkazy E2E:

- **(a) I5 kontrakt**: `risk.payloadHash` z response == `sha256sum` req souboru na disku, `hashSource: "raw-bytes"` (-24: `f6a1be7b…` shoda 1:1).
- **(b) T5-2 delete + potvrzení/zamítnutí**: delete → `confirm_required` (soubor v `pending\`, nonce 64 hex + hashPrefix v res, results `pending`) → popup pumpy → Ano → provedeno, element smazán, audit `confirmationRequired=true; confirmedByUser=true; channel=pumpa` + timestamp (-25); Ne → `E_RISK_REJECTED`, soubor v `rejected\`, audit `confirmedByUser=false` (-26), cíl nesmazán (kontrola -31).
- **(c) T5-3 integrita**: modifikace pending souboru mezi klasifikací a potvrzením (přidaná mezera) → `E_RISK_INTEGRITY`, nic neprovedeno, `rejected\`, audit `integrityFailed=1` (-27); cíl nesmazán (-31).
- **(d) T5-7 podvržené potvrzení**: top-level `nonce`+`payloadHash` v request JSON položeném přímo do `requests\` → `E_RISK_CONFIRM`, `rejected\`, žádný dialog (-28); tamper nonce v res souboru mezi zobrazením a klikem → executor odmítl (`E_RISK_CONFIRM`, dávka zůstala v `pending\`), dokumentovaná obnova (soubor zpět do `requests\` → nová klasifikace s novým nonce `983b0936…` ≠ `d7b5334d…`) ověřena, poté zamítnuto (-29). Samotný hash bez nonce nestačí (offline harness + executor kontroluje nonce první).
- **(e) T5-8 migrace E_QUOTA (W6)**: `clone_elements` 101× s `confirm: true` → warning „pole confirm ztratilo účinek…" v response, status `confirm_required` (ŽÁDNÉ `"code":"E_QUOTA"`, žádná chyba), ELEVATED cesta; zamítnuto bez klonování (-30).
- **(f) T5-13 W8**: dávka s `$5` (UNCERTAIN B3) → confirm → exekuce `E_EXCEPTION` (-32); první write po výjimce → `confirm_required` s JEDINÝM důvodem W8 (-33), po potvrzení provedeno a flag smazán — následný LOW write bez potvrzení (-34).
- **(g) T5-14 bezstavovost + model-state TOCTOU (W1)**: delete dávka -35 čekala v `pending\` přes timeout popupu (300 s — dávka správně dál čeká), **restart EA** (pumpa re-attach → znovunabídnuta, týž hash `7940403d…`) i **restart pumpy** (offerPendingOnStart); mezitím jiná potvrzená dávka smazala target (-36) → potvrzení -35 → re-klasifikace: „Neresolvovatelny target… fail-closed" + „Re-klasifikace při potvrzení: metriky se změnily (W1)", affectedElements 1→0, **nové `confirm_required` s novým nonce** — exekuce neproběhla; druhé kolo zamítnuto → `rejected\` (-35).
- **(h) deploy_src přes confirm cyklus**: deploy fixu gate = ELEVATED → potvrzeno → provedeno + reload kódu pumpy po potvrzení (-38).
- **(i) T5-10 GUI fallback (kanál `gui`) + T5-9 EA runtime**: klikací režim v EA (Specialize → AI Bridge → Process requests) — EA dialog Ano/Ne/Zrušit se souhrnem a hashPrefix, potvrzení → exekuce s auditem `channel=gui` (-39 potvrzeno+E_NOT_FOUND na již smazaném targetu — fail-closed ELEVATED zafungoval; -40 úklidový delete potvrzen a proveden). **Výkon EA runtime (T5-9/W5): hash 492 kB payloadu = 63 ms** (textová cesta; EA 17.1.5 JS je na FB_Sha256 výrazně rychlejší než JScript pumpy — 1300 ms). Objevena a opravena **nová dual-runtime past** (PROTOKOL §1a/4): binární VARIANT → MSXML `nodeTypedValue` hází v EA runtime nezachytitelnou COM chybu a shodí celý handler; EA runtime proto čte soubory textově utf-8 (`hashSource: "text-utf8"`, ≡ raw pro UTF-8 bez BOM), pumpa dál surové bajty (`raw-bytes`). Fix nasazen dávkou -41 (deploy_src přes confirm cyklus).
- **(j)** grep `ActiveXObject` na nových/změněných src souborech = 0 (jen komentáře); pending\ po testech prázdná; offline harness rozšířen o EA-runtime větev (57/57 PASS).

**Iterace 4 — vrátný / AI import režim (2026-08-20; spec = zadání iterace 4 v1.1 §2/§5/§9, PROTOKOL §6f/§13):** kontinuální kanál bez pumpy — Copy v Copilotu → vrátný (PS zrozený inline z EA) → schránka → `requests\` → `FB_Process` (Add-in Search) → `FB_Main` → chat verze do schránky. MCP protějšek neexistuje — bridge-only 🔵. Nové operace modelu: `FB_Gatekeeper` (kanon PS), `FB_GatekeeperLaunch` (launcher, StdIn carrier), `FB_Process` (shim Add-in Search), `FB_ChatRender` (chat verze), `FB_QcRun`+`FB_QcConfig` (QC v ACK). Registr operací beze změny (39) — vrátný ani FB_Process nejsou dosažitelné obsahem dávky.

- **(a) offline harness 49/49 PASS** (Node mock COM/EA, `/harness` mimo repo): syntaxe všech 87 src; `FB_ChatRender` 10 případů (OK+QC, query 0 řádků = T4-3c formulace, error index+§5a, confirm_required hashPrefix ANO / nonce+plný hash NIKDY, QC nález/nedoběhlo oddělené od zápisu, W10 rozpočet+ukazatel bez tichého cutu, rejected/blocked/integrity); `FB_QcRun` 5 stavů (čisté/nález se scope escapováním `''`/nedoběhlo/bez konfigurace/scope neznámý); `FB_Process` (ping→gk-ping s `repository`, W8 session dohnání, **guard: soubor v `pending\` odmítnut**, req→res+chat sidecar+processed, E_PARSE→rejected, confirm_required→res+chat+confirm sidecar bez nonce/plného hashe+pending\, **confirm→FB_ConfirmPending kanál `okno`** approve, res přepsán+confirm sidecar smazán); `FB_GatekeeperLaunch` (Exec, `powershell.exe -STA -Hidden -Command -` = **čistá příkazová řádka bez base64** — Norton fix, viz (c), parametry+reap defaulty, žádné #komentáře v rouře, gk-config override, debug→`-NoExit`); PS invarianty kanonu (párování `(){}[]`, žádné backtick/here-stringy, mutex W3 `Local\…v1_`+WaitOne(0)+abandoned+exit 2, reap exit 3+REATTACHING/REAPING, kanál `okno` přes FB_Process, druhá obrana nonce/payloadHash regex, dedup W5+Downloads+session-log, reap nesahá na schránku, **ASCII only**).
- **(b) grep `ActiveXObject`** na nových/změněných src (`FB_Gatekeeper`, `FB_GatekeeperLaunch`, `FB_Process`, `FB_ChatRender`, `FB_QcRun`, `FB_QcConfig`, `FB_Main`, `EA_GetMenuItems`, `EA_MenuClick`) = **0** (mimo komentáře/typeof); kanon vrátného je PS, COM v EA runtime jen přes `FB_ComObj`.
- **(c) nosič kódu (StdIn carrier)**: kanon 15,5 k > `-EncodedCommand` ~12 k (W2/R4 potvrzen). **⚠ První živé spuštění 2026-08-20: Norton `IDP.HELU.CMD.Generic24` „detekce pomocí příkazového řádku" zablokoval `powershell.exe`** kvůli base64 blobu z `-EncodedCommand` (blokovaný launch → `Exec` = undefined → chyba launcheru). **Oprava: `-Command -`** — příkazová řádka čistá, celý kanon rourou přes StdIn (`WScript.Shell.Exec` = CreateProcess). Drží principy §2; odchylka od §7/R4 „R1 fallback" zapsána do oznámení R5. **Chování-based AV/EDR (EA→powershell + schránka) = konkrétní vstup pro R5 podmínku C-A (DLP/EDR clearance).**
- **(d) živý klikací E2E = Miloš u stroje** (`docs/e2e-iterace4/PROTOKOL-E2E-ITERACE4.md`, dávky 20260820-*): potvrzení ELEVATED kanálem `okno` (audit `confirmChannel=okno`), zamítnutí, fronta 2 čekajících (I6), restart vrátného s pending\ dávkou (bezstavovost), mutex dvojspuštění (W3), dedup schránka×Downloads (W5). Reálný PS 5.1 parse: Cowork sandbox nemá PS ani síť do GitHubu — token-parse ověřen staticky (závorky/uvozovky/here-strings/ASCII), runtime u Miloše.

**Iterace 4 dodatek — clipboard režim (plně ruční fallback bez PowerShellu, 2026-08-20):** `FB_ClipboardImport` (menu „Zpracuj davku ze schranky") — reakce na Norton `CMD:Powershell-AP [Drp]` „Dropper" blok vrátného (persistentní PS clipboard watcher = infostealer signatura, W1). Běží celý uvnitř EA.exe, žádný powershell/watcher → AV-neviditelné. Copy → klik → EA čte schránku (COM `htmlfile`), materializuje+zpracuje (`FB_Main` I5), chat verzi vloží zpět do schránky; ELEVATED → EA dialog → `FB_ConfirmPending` kanál `gui`; druhá obrana B1 (odmítá obsah s nonce/payloadHash); dedup W5. Offline harness rozšířen na **55/55** (6 clipboard testů: bez signatury → výzva, nonce → odmítnuto, LOW done → res+archiv+chat zpět do schránky, dedup, ELEVATED → dialog → kanál gui). Registr operací beze změny (menu-only). **✅ Ověřeno naživo 2026-08-20 (eaexample):** dávky `20260820-c1`/`c2` (ping) přes Copy → menu → `EAFB OK …: 1/1 ops`, dedup W5 potvrzen. **⚠ Dual-runtime lekce: `htmlfile` `getData` v EA runtime funguje, `setData` tiše NE** (falešné „zapsáno", schránka drží původní) → `clipWrite` má read-back ověření + fallback `clip.exe` (temp UTF-16LE → `cmd /c clip`; deploy -07). Odpověď vždy i v EA dialogu. **✅ Ctrl+V ověřen naživo (dávka `c3`, deploy -07): po zpracování je ve schránce `EAFB OK 20260820-c3: 1/1 ops` — celý kruh Copy→klik→Ctrl+V funguje, bez powershellu.** **✅ Clipboard režim E2E KOMPLET naživo (20260820-c1…c6):** c1/c2/c3 ping + čtení/zápis schránky; **c4 LOW zápis + `QC NALEZ` (třístav — zápis proběhl, QC není chyba); c6 ELEVATED delete → potvrzovací dialog → Ano → `2/2 ops` (`FB_ConfirmPending` kanál gui) + `QC nedobehlo` (scope bez packages).** Confirm okruh v clipboard režimu potvrzen. (Pozn.: `delete_from_model` Element řeší jen guid/id, ne jméno — adresovat přes `$N`/guid. QC-DEMO scopuje celý package, hlásí i předexistující nález.)

**Iterace 5 — autorizace, zvýraznění v browseru, kontext výběru (2026-08-20; PROTOKOL §6g; harness `test/harness.js` 131/131 PASS, poprvé V REPU):** MCP protějšky neexistují (kromě `get_selected_context` ↔ `get_current_*`) — bridge-only 🔵. Živý klikací E2E = dávky 20260821-* u Miloše (`docs/e2e-iterace5/PROTOKOL-E2E-ITERACE5.md`).

- **(A) `FB_AccessGroups` + `FB_UserAccess` + `E_ADDIN_ACCESS`**: write fíčury za EA security skupinou, vynucení v executoru (všechny kanály); security off = vše povoleno (Milošovo rozhodnutí); fail-closed při každé nejistotě. Harness: 6 testů FB_UserAccess (off/člen/nečlen/bez konfigurace/SQL fail/cache) + 3 testy gate ve FB_Main. ⚠ Jména tabulek `t_secuser`/`t_secgroup`/`t_secuser_group` ověřit v bance čtecí dávkou (doma security vypnutá).
- **(B) Output proklik + `FB_Changes` + spike `FB_NavProbe`**: navigace na dotčené prvky VÝHRADNĚ nativními mechanismy EA (WriteOutput ElementID; ReportViewData CLASSGUID/CLASSTYPE) — žádné `ShowInProjectView` z add-inu (pád 2026-08-20). Harness: 3 testy FB_LogChanges (element id / package Element.ElementID / smazané id 0), 3 testy FB_Changes (id dávky / poslední dávka / prázdno), 1 test NavProbe (krokování). Živě: dvojklik v Output, definice searche FB_Changes, spike dle SPIKE-NAV.md.
- **(C) `get_selected_context` + `scope` na `find_*`** (`FB_InBranch` — parent řetěz, žádné dialektové SQL ani IN listy): klientské předání kontextu (Copilot vloží GUIDy; žádný server-side default — TOCTOU + nedeterministický retry). Harness: 5 testů kontextu (element/package/nic/multi/diagram) + 3 testy scope (filtr/pkg filtr/bez scope beze změny).
- grep `ActiveXObject` nové/změněné src = 0 (15 souborů).

**M365 části** (Downloads watcher, OneDrive/SharePoint responses, Confluence noha) — vědomě mimo rozsah, čekají na vyhodnocení POC fáze 3.
